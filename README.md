# MLLM
A Multimodal Time Series Foundation Model


## Requirements:
- Generate the text-embedding first:
```bash
script/gen_text_embedding.sh
```
Note: prediction length is set to 24 by default, you can change it in the script to match with your experiment in training.